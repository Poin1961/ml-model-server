[package]
name = "ml-model-server"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ndarray = "0.15"
ort = { version = "1.15", features = ["ndarray"] } # ONNX Runtime bindings
